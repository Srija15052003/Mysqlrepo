#SELECT COUNT(Percentage) FROM sbya.countrylanguage WHERE Percentage > 90.0
#SELECT SUM(Percentage) FROM sbya.countrylanguage
#SELECT Avg(Percentage) FROM sbya.countrylanguage
#SELECT Max(Percentage) FROM sbya.countrylanguage
SELECT Min(Percentage) FROM sbya.countrylanguage